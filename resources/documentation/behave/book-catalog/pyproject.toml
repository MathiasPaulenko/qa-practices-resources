[project]
name = "book-catalog"
version = "0.1.0"
description = "Runnable Behave example with in-memory, HTTP API and SQLite catalog variants"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "behave>=1.3.0",
    "requests>=2.32.0",
    "playwright>=1.62.0,<2.0",
]
