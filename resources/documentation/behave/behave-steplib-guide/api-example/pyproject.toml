[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "behave-steplib-example"
version = "0.1.0"
description = "Minimal example for behave-steplib 1.5.1"
requires-python = ">=3.11"
dependencies = [
    "behave>=1.3.0,<1.4",
    "behave-steplib[api,io,data]>=1.5.1",
]
